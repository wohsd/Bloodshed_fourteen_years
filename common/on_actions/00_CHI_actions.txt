on_actions = {
    on_startup = {
        effect = {
            CHI = {
                # 初始化派系影响力变量
                # 基础影响力（可通过国策、决议增加）
                set_variable = { CHI_he_power_base = 0 }
                set_variable = { CHI_chen_power_base = 0 }
                set_variable = { CHI_hu_power_base = 0 }
                set_variable = { CHI_tang_power_base = 0 }
                
                # 技能影响力（基于将领等级）
                set_variable = { CHI_he_power_skill = 0 }
                set_variable = { CHI_chen_power_skill = 0 }
                set_variable = { CHI_hu_power_skill = 0 }
                set_variable = { CHI_tang_power_skill = 0 }
                
                # 总影响力
                set_variable = { CHI_he_power = 0 }
                set_variable = { CHI_chen_power = 0 }
                set_variable = { CHI_hu_power = 0 }
                set_variable = { CHI_tang_power = 0 }
                set_variable = { CHI_total_clique_power = 0 }
                
                # 初始化动态修正变量默认值
                # 何应钦派系
                set_variable = { CHI_he_supply_consumption = -0.05 } # 减少补给损耗
                set_variable = { CHI_he_supply_range = 0.05 } # 扩大补给范围
                set_variable = { CHI_he_planning_speed = -0.05 } # 减少计划速度
                
                # 陈诚派系
                set_variable = { CHI_chen_army_exp = 0.05 } # 增加陆军经验增长
                set_variable = { CHI_chen_political_power = -0.02 } # 减少政治点获取
                
                # 胡宗南派系
                set_variable = { CHI_hu_army_org = 0.5 } # 增加陆军组织度
                set_variable = { CHI_hu_commander_exp = -0.05 } # 减少指挥官经验
                set_variable = { CHI_hu_attack_vs_japan = -0.05 } # 减少对日本攻击
                set_variable = { CHI_hu_defense_vs_japan = -0.05 } # 减少对日本防御
                
                # 汤恩伯派系
                set_variable = { CHI_tang_irregular_attack = 0.05 } # 增加非正规军攻击
                set_variable = { CHI_tang_irregular_defence = 0.05 } # 增加非正规军防御
                set_variable = { CHI_tang_irregular_org = 0.05 } # 增加非正规军组织度
                set_variable = { CHI_tang_industrial = -0.05 } # 减少工厂产出
                
                # 计算技能影响力（基于将领等级）
                every_army_leader = {
                    limit = { has_trait = heyingqinxi }
                    add_to_variable = { PREV.CHI_he_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = chenchengxi }
                    add_to_variable = { PREV.CHI_chen_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = huzongnanxi }
                    add_to_variable = { PREV.CHI_hu_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = tangenboxi }
                    add_to_variable = { PREV.CHI_tang_power_skill = skill }
                }
                
                every_navy_leader = {
                    limit = { has_trait = heyingqinxi }
                    add_to_variable = { PREV.CHI_he_power_skill = skill }
                }
                every_navy_leader = {
                    limit = { has_trait = chenchengxi }
                    add_to_variable = { PREV.CHI_chen_power_skill = skill }
                }
                every_navy_leader = {
                    limit = { has_trait = huzongnanxi }
                    add_to_variable = { PREV.CHI_hu_power_skill = skill }
                }
                every_navy_leader = {
                    limit = { has_trait = tangenboxi }
                    add_to_variable = { PREV.CHI_tang_power_skill = skill }
                }
                
                # 计算总影响力 = 基础影响力 + 技能影响力
                set_variable = { CHI_he_power = CHI_he_power_base }
                add_to_variable = { CHI_he_power = CHI_he_power_skill }
                
                set_variable = { CHI_chen_power = CHI_chen_power_base }
                add_to_variable = { CHI_chen_power = CHI_chen_power_skill }
                
                set_variable = { CHI_hu_power = CHI_hu_power_base }
                add_to_variable = { CHI_hu_power = CHI_hu_power_skill }
                
                set_variable = { CHI_tang_power = CHI_tang_power_base }
                add_to_variable = { CHI_tang_power = CHI_tang_power_skill }
                
                # 计算总派系影响力
                set_variable = { CHI_total_clique_power = 0 }
                add_to_variable = { CHI_total_clique_power = CHI_he_power }
                add_to_variable = { CHI_total_clique_power = CHI_chen_power }
                add_to_variable = { CHI_total_clique_power = CHI_hu_power }
                add_to_variable = { CHI_total_clique_power = CHI_tang_power }
                
                # 转换为百分比（避免除以0）
                if = {
                    limit = { check_variable = { CHI_total_clique_power > 0 } }
                    set_variable = { CHI_he_power_percent = CHI_he_power }
                    divide_variable = { CHI_he_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_he_power_percent = 100 }
                    
                    set_variable = { CHI_chen_power_percent = CHI_chen_power }
                    divide_variable = { CHI_chen_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_chen_power_percent = 100 }
                    
                    set_variable = { CHI_hu_power_percent = CHI_hu_power }
                    divide_variable = { CHI_hu_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_hu_power_percent = 100 }
                    
                    set_variable = { CHI_tang_power_percent = CHI_tang_power }
                    divide_variable = { CHI_tang_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_tang_power_percent = 100 }
                }
                else = {
                    set_variable = { CHI_he_power_percent = 25 }
                    set_variable = { CHI_chen_power_percent = 25 }
                    set_variable = { CHI_hu_power_percent = 25 }
                    set_variable = { CHI_tang_power_percent = 25 }
                }
                
                # 应用派系影响力效果
                # 何应钦派系
                if = {
                    limit = { check_variable = { CHI_he_power_percent > 50 } }
                    # 高影响力 - 更好的buff和更严重的debuff
                    set_variable = { CHI_he_supply_consumption = -0.15 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.15 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.15 } # 减少计划速度
                }
                else_if = {
                    limit = { check_variable = { CHI_he_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_he_supply_consumption = -0.1 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.1 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.1 } # 减少计划速度
                }
                else {
                    # 低影响力
                    set_variable = { CHI_he_supply_consumption = -0.05 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.05 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.05 } # 减少计划速度
                }
                
                # 陈诚派系
                if = {
                    limit = { check_variable = { CHI_chen_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_chen_army_exp = 0.15 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.1 } # 减少政治点获取
                }
                else_if = {
                    limit = { check_variable = { CHI_chen_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_chen_army_exp = 0.1 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.05 } # 减少政治点获取
                }
                else {
                    # 低影响力
                    set_variable = { CHI_chen_army_exp = 0.05 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.02 } # 减少政治点获取
                }
                
                # 胡宗南派系
                if = {
                    limit = { check_variable = { CHI_hu_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_hu_army_org = 1.5 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.15 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.15 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.15 } # 减少对日本防御
                }
                else_if = {
                    limit = { check_variable = { CHI_hu_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_hu_army_org = 1.0 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.1 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.1 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.1 } # 减少对日本防御
                }
                else {
                    # 低影响力
                    set_variable = { CHI_hu_army_org = 0.5 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.05 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.05 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.05 } # 减少对日本防御
                }
                
                # 汤恩伯派系
                if = {
                    limit = { check_variable = { CHI_tang_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_tang_industrial = -0.15 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.15 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.15 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.15 } # 非正规军组织度加成
                }
                else_if = {
                    limit = { check_variable = { CHI_tang_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_tang_industrial = -0.1 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.1 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.1 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.1 } # 非正规军组织度加成
                }
                else {
                    # 低影响力
                    set_variable = { CHI_tang_industrial = -0.05 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.05 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.05 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.05 } # 非正规军组织度加成
                }
            }
        }
    }
    

    
    # 每月检测将领等级并更新派系影响力
    on_monthly = {
        effect = {
            CHI = {
                # 直接设置技能影响力为相应将领的等级（只计算陆军将领）
                set_variable = { CHI_he_power_skill = 0 }
                set_variable = { CHI_chen_power_skill = 0 }
                set_variable = { CHI_hu_power_skill = 0 }
                set_variable = { CHI_tang_power_skill = 0 }
                
                # 计算陆军将领的技能影响力
                every_army_leader = {
                    limit = { has_trait = heyingqinxi }
                    add_to_variable = { PREV.CHI_he_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = chenchengxi }
                    add_to_variable = { PREV.CHI_chen_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = huzongnanxi }
                    add_to_variable = { PREV.CHI_hu_power_skill = skill }
                }
                every_army_leader = {
                    limit = { has_trait = tangenboxi }
                    add_to_variable = { PREV.CHI_tang_power_skill = skill }
                }
                
                # 计算总影响力 = 基础影响力 + 技能影响力
                set_variable = { CHI_he_power = CHI_he_power_base }
                add_to_variable = { CHI_he_power = CHI_he_power_skill }
                
                set_variable = { CHI_chen_power = CHI_chen_power_base }
                add_to_variable = { CHI_chen_power = CHI_chen_power_skill }
                
                set_variable = { CHI_hu_power = CHI_hu_power_base }
                add_to_variable = { CHI_hu_power = CHI_hu_power_skill }
                
                set_variable = { CHI_tang_power = CHI_tang_power_base }
                add_to_variable = { CHI_tang_power = CHI_tang_power_skill }
                
                # 计算总派系影响力
                set_variable = { CHI_total_clique_power = 0 }
                add_to_variable = { CHI_total_clique_power = CHI_he_power }
                add_to_variable = { CHI_total_clique_power = CHI_chen_power }
                add_to_variable = { CHI_total_clique_power = CHI_hu_power }
                add_to_variable = { CHI_total_clique_power = CHI_tang_power }
                
                if = {
                    limit = { check_variable = { CHI_total_clique_power > 0 } }
                    set_variable = { CHI_he_power_percent = CHI_he_power }
                    divide_variable = { CHI_he_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_he_power_percent = 100 }
                    
                    set_variable = { CHI_chen_power_percent = CHI_chen_power }
                    divide_variable = { CHI_chen_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_chen_power_percent = 100 }
                    
                    set_variable = { CHI_hu_power_percent = CHI_hu_power }
                    divide_variable = { CHI_hu_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_hu_power_percent = 100 }
                    
                    set_variable = { CHI_tang_power_percent = CHI_tang_power }
                    divide_variable = { CHI_tang_power_percent = CHI_total_clique_power }
                    multiply_variable = { CHI_tang_power_percent = 100 }
                }
                else {
                    set_variable = { CHI_he_power_percent = 25 }
                    set_variable = { CHI_chen_power_percent = 25 }
                    set_variable = { CHI_hu_power_percent = 25 }
                    set_variable = { CHI_tang_power_percent = 25 }
                }
                
                # 应用派系影响力效果
                # 何应钦派系
                if = {
                    limit = { check_variable = { CHI_he_power_percent > 50 } }
                    # 高影响力 - 更好的buff和更严重的debuff
                    set_variable = { CHI_he_supply_consumption = -0.15 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.15 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.15 } # 减少计划速度
                }
                else_if = {
                    limit = { check_variable = { CHI_he_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_he_supply_consumption = -0.1 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.1 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.1 } # 减少计划速度
                }
                else {
                    # 低影响力
                    set_variable = { CHI_he_supply_consumption = -0.05 } # 减少补给损耗
                    set_variable = { CHI_he_supply_range = 0.05 } # 扩大补给范围
                    set_variable = { CHI_he_planning_speed = -0.05 } # 减少计划速度
                }
                
                # 陈诚派系
                if = {
                    limit = { check_variable = { CHI_chen_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_chen_army_exp = 0.15 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.1 } # 减少政治点获取
                }
                else_if = {
                    limit = { check_variable = { CHI_chen_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_chen_army_exp = 0.1 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.05 } # 减少政治点获取
                }
                else {
                    # 低影响力
                    set_variable = { CHI_chen_army_exp = 0.05 } # 增加陆军经验增长
                    set_variable = { CHI_chen_political_power = -0.02 } # 减少政治点获取
                }
                
                # 胡宗南派系
                if = {
                    limit = { check_variable = { CHI_hu_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_hu_army_org = 1.5 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.15 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.15 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.15 } # 减少对日本防御
                }
                else_if = {
                    limit = { check_variable = { CHI_hu_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_hu_army_org = 1.0 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.1 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.1 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.1 } # 减少对日本防御
                }
                else {
                    # 低影响力
                    set_variable = { CHI_hu_army_org = 0.5 } # 增加陆军组织度
                    set_variable = { CHI_hu_commander_exp = -0.05 } # 减少指挥官经验
                    set_variable = { CHI_hu_attack_vs_japan = -0.05 } # 减少对日本攻击
                    set_variable = { CHI_hu_defense_vs_japan = -0.05 } # 减少对日本防御
                }
                
                # 汤恩伯派系
                if = {
                    limit = { check_variable = { CHI_tang_power_percent > 50 } }
                    # 高影响力
                    set_variable = { CHI_tang_industrial = -0.15 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.15 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.15 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.15 } # 非正规军组织度加成
                }
                else_if = {
                    limit = { check_variable = { CHI_tang_power_percent >= 25 } }
                    # 中影响力
                    set_variable = { CHI_tang_industrial = -0.1 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.1 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.1 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.1 } # 非正规军组织度加成
                }
                else {
                    # 低影响力
                    set_variable = { CHI_tang_industrial = -0.05 } # 减少工厂产出
                    set_variable = { CHI_tang_irregular_attack = 0.05 } # 非正规军攻击加成
                    set_variable = { CHI_tang_irregular_defence = 0.05 } # 非正规军防御加成
                    set_variable = { CHI_tang_irregular_org = 0.05 } # 非正规军组织度加成
                }
            }
        }
    }
}


